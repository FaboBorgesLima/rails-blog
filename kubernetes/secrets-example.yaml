apiVersion: v1
kind: Secret
metadata:
    name: blog-secrets
type: Opaque
stringData:
    DB_HOST: "${DB_HOST}"
    DB_NAME: "${DB_NAME}"
    DB_USERNAME: "${DB_USERNAME}"
    DB_PASSWORD: "${DB_PASSWORD}"
    RAILS_MASTER_KEY: "${RAILS_MASTER_KEY}"
    REDIS_URL: "${REDIS_URL}"
